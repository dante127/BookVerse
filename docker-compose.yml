services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: bookverse-sqlserver
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - mssql_data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'Password123!' -C -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - bookverse-network

  redis:
    image: redis:7-alpine
    container_name: bookverse-redis
    restart: unless-stopped
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - bookverse-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bookverse-api
    restart: unless-stopped
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_HTTP_PORTS=8080
      - ConnectionStrings__DefaultConnection=Server=mssql,1433;Database=BookVerseDb;User Id=sa;Password=Password123!;TrustServerCertificate=True;MultipleActiveResultSets=true;
      - Redis__Configuration=redis:6379
      - Jwt__Issuer=BookVerse
      - Jwt__Audience=BookVerseAudience
      - Jwt__SecretKey=SuperSecretProductionKeyThatMustBeAtLeast32BytesLongForHmacSha256!
      - Jwt__AccessTokenExpirationMinutes=15
      - Jwt__RefreshTokenExpirationDays=7
    depends_on:
      mssql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - bookverse-network

networks:
  bookverse-network:
    driver: bridge

volumes:
  mssql_data:
  redis_data:
